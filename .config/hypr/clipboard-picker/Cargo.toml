[package]
name = "clipboard-picker"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk = "0.18"
glib = "0.18"
gdk = "0.18"
gdk-pixbuf = "0.18"
gtk-layer-shell = { version = "0.8", features = ["v0_6"] }
serde_json = "1"
libc = "0.2"

[profile.release]
opt-level = 2
lto = true
codegen-units = 1
