[Unit]
Description=Keep Wayland clipboard/primary selection alive after the source app closes
After=graphical-session.target
Wants=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/wl-clip-persist --clipboard both
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
